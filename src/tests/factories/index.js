import { buildAuthor, createAuthor } from './author.factory';
import { buildBook, createBook } from './book.factory';
import { buildGenre, createGenre } from './genre.factory';
import { buildTag, createTag } from './tag.factory';

export {
  buildBook,
  createBook,
  buildAuthor,
  createAuthor,
  buildGenre,
  createGenre,
  buildTag,
  createTag,
};
